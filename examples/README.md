# Example roles for pi-agent-roles

These example role files are intended for live testing.

The repository-local `.pi/roles/` directory contains symlinks to the files in
`examples/roles/`, so pi will discover them automatically while you test the
extension inside this repo.
